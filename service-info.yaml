# the schema of this file is still WIP, changes will be applied/merged automatically
name: firebase-service
description: |-
  This is firebase-service service.
  The description can be multiline but please keep it short.
criticality: high
visibility: external
tech-stack: backend
team-name: huat
code-repository: https://gitlab.com/affinidi/safe-travel/huat/health-folder/firebase-service.git
endpoints:
  health: /health
  metrics: /metrics
  api: /api
  api-docs: /api-docs
target-envs:
  - affinity-project-dev
  - affinity-project-stg
  - affinity-project-prod
config:
  - ci-ci:
      - pipeline-auto-update: false
